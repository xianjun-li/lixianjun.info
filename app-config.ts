export const taxonomies = [
    { name: "categories", type: "taxonomy" },
    { name: "series", type: "controlled" },
    { name: "tags", type: "tag" }
]

export const paging = {
    // 每页记录数
    offset: 10,
}